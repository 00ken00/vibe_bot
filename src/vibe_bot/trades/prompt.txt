I want to implement a arbitrage bot between bitbank and bitflyer:

The basic strategy is below:

- consider the symbol position bitbank and bitflyer as a whole:
  buy in bitbank and sell in bitflyer is regarded as BUY action
  sell in bitbank and buy in bitflyer is regarded as SELL action
  - the BUY price is the cost of BUY action:
    namely buy_price in bitbank - sell_price in bitflyer
  - the SELL price is the profit of SELL action:
    namely sell_price in bitbank - buy_price in bitflyer

- Set a positive threshold value
  - if the BUY price is below -threshold, perform BUY action and enter a long position
  - if the SELL price is above 0, perform SELL action and close this long position
  - if the SELL price is above threshold, perform SELL action and enter a short position
  - if the BUY price is below 0, perform BUY action and close this short position

- Only maintain one maker in bitbank:
  - For example in 0 position, whether place a BUY order or SELL order should dependent on which order has higher probability to be filled.
  - the maker order should updated for 0.5 second interval (set this also a parameter)


Implement a web app frontend that can monitor the price by plotting the realtime chart for the BUY and SELL price realtime (update per sec).
The active maker order should be plot in the same chart.

Also add some other key results for monitoring in the web app. (like profit)

Log the key transaction information for further analysis, like order slippage.

Implement all scripts under src/trades folder


/Users/ken/Projects/vibe_bot/src/vibe_bot/trades/bitbank_bitflyer_arbitrage.py
is too long, split the html or web app part into a different file



for /Users/ken/Projects/vibe_bot/src/vibe_bot/trades/bitbank_bitflyer_arbitrage.py
Add class docs for explaining what this class do


Will dry-run place actual maker to exchange?



add make file that make the run more easily


is this maker file redundant?
I want to add the script to readme.md


remove the makefile



Is current code using websocket to get quote data from bitbank and bitflyer? 



Change current ticker logic to using websocket, since websocket has shorter latency.
Also using orderbook instead of ticker to consider the exchange depth.
the quote data should also consider order_size as depth. 
instead of just using best ask / best bid, estimate the order price to full fill the order size.


Is quote_interval still used in the bot?



In src/vibe_bot/trades/bitbank_bitflyer_web.py

the config has type = any, makes my editor failed to recognize the type, change it to correct type, avoid all any type for better static parsing



Now the dry run failed, fix it



When I open the web page: http://127.0.0.1:8766/
I got following error:

Failed to open a WebSocket connection: invalid Connection header: keep-alive.

You cannot access a WebSocket server directly with a browser. You need a WebSocket client.


Now make the plot in web that the x axis range is fixed length (range = 1min), the new data appears to the right and move to left, instead of spread over x axis



Add a threshold offset parameter, so that for example the open long position condition changes from buy_price > -threshold to buy_price > threshold_offset-threshold



I see a horizontal dashed line in the chart in web app, what is it?



Add a collapsed panel in webapp that shows all parameter values


move the parameter panel to the bottom of the web page


remove the collapsablilty, make the panel always visible


what is action on web app? what are possible value in action?


Add this help info to website



Change last_action to a enum class and add description



Move the description of action to a hover tooltip style, only mouse hover on the action value, the description will show up, otherwise it will be hidden


The tooltip overflows the viewport, fix it


How is bitbank_bid bitbank_ask used in this code? (not bitbank_bid_vwap)



When I run with --live option, I encounter the placing order error.
Implement a minimal maker place code in scratch.py file, so that I can test it



rewrite the scratch code, do not use any parse arg, use direct arguments, that is more
easily used in ipython


remove __file__ part since it is not runnable in ipython


Why the BUY maker order using quote.bitbank_bid + self.config.tick_size,
shouldn't it use bitbank_ask - self.config.tick_size? 



But   bitbank_bid + tick_size
may >=
bitbank_ask

so that the maker order can not be placed,
use bitbank_ask - tick_size instead



what is 

        if price >= quote.bitbank_ask or price <= quote.bitbank_bid:
            return None
?


should not it depedent on the side?



Currently the log does not contain the order placing attempting,
add an order placing log before the actual placing code, so that I can retrieve what order price size or etc. is attempted to place



Add event before any bitbank / bitflyer private API call for tracing


draw a flowchart or sequence diagram for the trade logic, including the condition and timing of each API call and log



draw using mermaid markdown and create a flowchart.md under the same directory


I got this rich text error:

Unable to render rich display

Parse error on line 5:
... = floor_to_tick min(passive, profitable
-----------------------^
Expecting 'SQE', 'DOUBLECIRCLEEND', 'PE', '-)', 'STADIUMEND', 'SUBROUTINEEND', 'PIPE', 'CYLINDEREND', 'DIAMOND_STOP', 'TAGEND', 'TRAPEND', 'INVTRAPEND', 'UNICODE_TEXT', 'TEXT', 'TAGSTART', got 'PS'

For more information, see https://docs.github.com/get-started/writing-on-github/working-with-advanced-formatting/creating-diagrams#creating-mermaid-diagrams



it is currently using _log_private_api_call before every api call and the parameters are
rewrite every time, which is a little redundant.
Can you find a way to log every private api call with parameters and raw response globally?
